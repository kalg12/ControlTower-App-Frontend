# Backend origin (no trailing slash). Leave unset in dev to use Vite proxy (/api → localhost:8080).
# Production: set to your API host; must be listed in backend CORS_ALLOWED_ORIGINS.
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=Control Tower
VITE_WS_URL=ws://localhost:8080/ws
